steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-f'
      - 'AELP2/apps/dashboard/Dockerfile'
      - '-t'
      - 'gcr.io/$PROJECT_ID/${_SERVICE}:${_IMAGE_TAG}'
      - '.'
images:
  - 'gcr.io/$PROJECT_ID/${_SERVICE}:${_IMAGE_TAG}'
options:
  logging: CLOUD_LOGGING_ONLY
